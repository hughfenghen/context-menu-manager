'use babel';

import ContextMenuManagerView from '../lib/context-menu-manager-view';

describe('ContextMenuManagerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
